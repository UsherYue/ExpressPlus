php


1、单元测试
2、善用文档
3、PHPSESSIONID     session/cookie关系    session集群共享
4、ORM实现原理  PHP Class Object=>Mysql 的映射过程。
4、TP连贯查询只是一个sql构造器
5、注解路由
6、PHP反射
7、trait 新特性的使用
trait foo { }

class bar {
  use foo;
}

print_r(class_uses(new bar));

print_r(class_uses('bar'));

function __autoload($class_name) {
   require_once $class_name . '.php';
}

// use __autoload to load the 'not_loaded' class
print_r(class_uses('not_loaded', true));

8、TTP参数接受  
9、RESTFUL API开发    请求方法即动作。

10、 队列 先进先出FIFO      
     生产者消费者
      sysvmsg  



11、Composer  等于node.js 下的 nam  属于包管理

    php-resque 

12、引用 传值  

变量存储于栈

function test(&$v){}

function test($v){}

13、 数据读写分离

14、http://pear.php.net/    C扩展
    http://pecl.php.net/    PHP类库区别


15、正则表达式

16、CSS hack
